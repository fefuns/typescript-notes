/**
 * type annotation
 * 类型注解：自己告诉TS变量是什么类型
 */
let count1: number;
count1 = 123;

/**
 * type inference
 * 类型推断: TS 会自动去尝试分析变量的类型
 */
let count2 = 123;
/**
 * 把鼠标移动到关键词上，看上面编辑器是否会自动显示类型
 * 如果 TS 能够自动分析变量类型，就什么都不需要做了。
 */
